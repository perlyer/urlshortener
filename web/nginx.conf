server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: любой маршрут (в т.ч. /stats/{code}) отдаёт index.html,
    # роутинг разруливает React Router на клиенте.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
